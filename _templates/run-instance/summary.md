# <server-name> — summary

The super compact log of what happened. The agent appends **one line per verified step** (after the human confirms), newest at the bottom. Format: `date  step  outcome`. No prose, no command output — details stay in the level files and cards.

## Log
<example: `2026-08-17  00-01  ✅ key-only ssh works, ed25519`>

## Incidents & adaptations
<Anything that deviated from the cards: OS adaptations, failures + recoveries, deliberate skips with reasons. Future-you reads exactly this section. If a deviation would help other servers too, open a PR or issue upstream.>
